// builder.js

const configLoader = require("./config.js");
const treeLoader = require("./tree.js");

function buildPath(path) {

}

function buildTreeWithConfig(tree,config) {

}

module.exports = {
    buildPath: buildPath,
    buildTreeWithConfig: buildTreeWithConfig
};
